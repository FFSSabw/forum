<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ffssabcloud.forum.domain.DB1.RoleRepository">
    <resultMap id="roleMap" type="com.ffssabcloud.forum.domain.Role">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
    </resultMap>
    <select id="findRolesById" parameterType="Integer" resultMap="roleMap">
        select r.name,
        r.id
        from user_role_rel urr
        left join roles r on r.id = urr.role_id
        where urr.user_id=#{id}
    </select>
</mapper>